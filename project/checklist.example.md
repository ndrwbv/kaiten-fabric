<!--
На что смотреть в коде именно вашего проекта. Один список на двоих: исполнитель
проходит его сам перед коммитом, ревьювер по нему же ревьюит. Поэтому список здесь
один — иначе они разъезжаются, и ревью превращается в спор о вкусах.

Что сюда писать: правила, у которых есть опора в ваших файлах правил или в реальной
поломке. «Так принято» и «мне так больше нравится» сюда не надо — агент выполнит
и это тоже, а ревью будет возвращать людям карточки из-за вкусовщины.

Ниже — рыба. Выкинь то, чего у вас нет, и допиши то, на чём вы реально горели.
Пустой файл — рабочий вариант: тогда агент опирается на общие соображения.
-->

### Тесты

Изменил логику — написал тест. Где лежат тесты и как называются: рядом с модулем,
`*.spec.ts`. Что мокается обязательно, а что не тестируется вовсе.

### Производительность

Что в этом проекте дорого: лишние ререндеры, тяжёлые вычисления в горячем пути,
N+1 в запросах, отсутствие пагинации, новая тяжёлая зависимость ради одной функции.

### Конфигурация вместо ветвлений

Если в проекте несколько брендов, стран, тарифов или инсталляций — здесь пишется,
что ветвиться по ним прямо в коде нельзя и куда отличия выносятся вместо этого.

### Баги

Граничные случаи, пустые и `undefined` значения, гонки, необработанные ошибки,
проглоченные исключения.

### Имена

Имена целиком, без сокращений. Имя должно читаться вслух.

### Ничего под ковёр

Никаких заглушенных линтеров, `@ts-ignore`, `any`, закомментированного кода,
отладочных логов, `TODO` без ссылки на задачу.

### Объём

Только то, что просили. Попутные рефакторинги, переформатирование чужого кода
и обновления зависимостей в дифф не попадают.
